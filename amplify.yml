version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - ng build --prod
  artifacts:
    baseDirectory: dist/App
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
